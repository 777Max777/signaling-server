version: '3.8'

services:
  server:
    build: .
    container_name: yjs-websocket-server
    restart: unless-stopped
    ports:
      - "1234:1234"
    environment:
      - NODE_ENV=production
    networks:
      - yjs-network

networks:
  yjs-network:
    driver: bridge
