FROM node:20-alpine

WORKDIR /app

# Copy package file
COPY package-y-websocket.json package.json

# Install dependencies
RUN npm install --production

# Copy server code
COPY y-websocket-server.js ./

EXPOSE 1234

CMD ["node", "y-websocket-server.js"]
